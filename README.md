# Detection-Pipeline-Cloudtrail-Elastic-Project
A small AWS security monitoring lab that collects AWS CloudTrail events, ships them into a SIEM, writes detection rules, generates safe test events, and documents the alerts like a real SOC project.
